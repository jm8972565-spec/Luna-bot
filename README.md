# Luna Bot V3

AI-powered Luna content generator.

Deployment:
1. Deploy this folder to a serverless Python host.
2. Set the environment variable OPENAI_API_KEY on the server.
3. Never put the API key in index.html or share it in chat.
4. Open the deployed URL on iPhone.

The browser calls /api/generate; the server calls the AI API, keeping the key private.
